# Separar cacenas (split)

datos = 'Hola Mundo'
lista = datos.split() # por default separa cada elemento por espacios en blanco
print(lista)

datos = 'Vicente,19,España'
lista = datos.split(',')
print(lista)